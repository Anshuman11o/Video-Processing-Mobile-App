# Video-Processing-Mobile-App
An offline-first mobile video app. Record short clips with weak network, and the app queues videos locally and uploads in the background, resuming from the exact byte it stopped at even after app is killed. A backend pipeline then transcodes, transcribes and packages each clip into an adaptive-bitrate reel waiting the next time you open the app.
